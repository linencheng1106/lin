## 题目1：

首次接触环形队列，学习环形队列学了好久好久我真的要崩溃了好难理解，看了多个b站视频后遂理解，题目1本身难度不大

## 题目2：

如何把闪烁次数和 LED 掩码组合进一个字节考虑了很久

## 题目3：

混淆了普通变量和指针，编译时出现了未定义的CMD_BLINK_MASK

### 1. CAN 标准帧包含哪些字段？各占多少位？

CAN 标准数据帧包括：

- 帧起始 SOF：1 位
- 标识符 ID：11 位
- RTR：1 位
- IDE：1 位
- 保留位 r0：1 位
- 数据长度 DLC：4 位
- 数据段：0～64 位，即 0～8 字节
- CRC 校验：15 位
- CRC 界定符：1 位
- ACK：2 位
- 帧结束 EOF：7 位

### 2. CAN 的 CRC 与本作业校验和有什么异同？

相同点是：两者都用于检查数据在传输过程中有没有出错。

不同点是：题目中的校验和主要通过数据相加得到，计算比较简单，但检错能力较弱；CAN 使用 CRC 多项式进行校验，计算更复杂，能发现更多类型的错误。

### 3. 如何设计机器人电机控制帧？

我会用 CAN ID 区分消息类型和电机编号：

```
ID = 优先级 + 命令类型 + 电机编号
```

数据段可以简单划分为：

```text
Byte 0：控制模式
Byte 1：使能、急停等标志
Byte 2～3：目标速度或目标位置
Byte 4～5：最大电流或最大转矩
Byte 6：命令序号
Byte 7：保留或校验和
```

急停命令使用较小的 CAN ID，使它拥有更高的发送优先级。
